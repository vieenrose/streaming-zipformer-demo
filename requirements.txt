# Sherpa-ONNX ASR Demo - Python Dependencies (Step 9: Docker Compose)
# Generated for Docker Compose environment

# Core ASR engine
sherpa-onnx>=1.12.20

# Audio processing
sounddevice>=0.5.3
soundfile>=0.13.1
numpy>=2.3.5

# Scientific computing (signal processing)
scipy>=1.13.0

# Chinese conversion (zh-CN ↔ zh-TW)
opencc-python-reimplemented>=0.1.7

# Terminal display width (for CJK/Arabic alignment)
wcwidth>=0.2.14

# Note: ten-vad with ONNX runtime is built manually in Dockerfile
# (Not available on PyPI - requires custom build)
